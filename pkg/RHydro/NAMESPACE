useDynLib(RHydro)

export(NSeff,
       get.data.types,
       get.stations,
       get.runCount,
       get.names,
       rhydro,
       topidx,
       topidx.classes,
       topmodel,
       wasim.data.types,
       rhydro.data.types,
       plot_rainfall.runoff,
       wasim.read.Run,
##       "$.HydroRun",
	long.wave.radiation.balance,
        sat.vap.pres
	)

exportClasses(
##              HydroRun,
              RHydro,
              HydroModelParameters,
              HydroTopmodelParameters,
              HydroWasimParameters)

exportMethods(print, plot, merge, coerce, validity.check, subset, simulate)
# import packages that define generics for the exported methods
# (e.g. stats for simulate)
import(methods, stats) 
#unclear what to import, removed graphics, sp
importFrom("graphics", "plot")


